//Strings all the values inside the double quote "".
var Str = "Hello"; //string
var five = "5"; // sting

// Number are the values which are +ve, -ve, integer, decimals, all natural numbers, zero , NAN(Not a Number)
var num = 12; // number
var notANumber = NaN; // number

// Boolean have two values "TRUE" or "FALSE".

var TrueOrFalse = true; // boolean
var notTRue = false; // boolean

// Undefined is used whn we want to declare the variable but we dont want to store any data at that particular time.
var hobby = undefined; // undefined

// Null is used when we dont want to store any data inside that variable.

var nothing = null; // null

// Non Premitive DataType

var arr = ["1", 3, null, "hello"];
var obj = {
  name: "dev",
  age: 22,
  class: "mca",
  languages: [english, hindi, punjabi],
};
